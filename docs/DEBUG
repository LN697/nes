<h1>Architectural Issues</h1>

Issues that can't be fixed with a simple "hack". Need to refactor the entire project.

* If the RDY line goes low 2 cycles before the write cycle, the target address of the instruction was not correct. (SHA, SHX, SHY, and SHS)